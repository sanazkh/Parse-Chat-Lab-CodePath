'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.resize = exports.combine = undefined;

var _gm = require('gm');

var _gm2 = _interopRequireDefault(_gm);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var imageMagick = _gm2.default.subClass({ imageMagick: true });

var minSize = 40;
var maxSize = 400;

var _clamp = function _clamp(num) {
  return Math.min(Math.max(num, minSize), maxSize);
};

var _parseSize = function _parseSize(size) {
  var ref = size.split('x');
  var width = ref[0];
  var height = ref[1];

  if (height === null) height = width;

  return {
    width: _clamp(width),
    height: _clamp(height)
  };
};

var combine = exports.combine = function combine(face, size, callback) {
  if (size) {
    size = _parseSize(size);
  } else {
    size = { width: maxSize, height: maxSize };
  }

  imageMagick().quality(0).in(face.eyes).in(face.nose).in(face.mouth).background(face.color).mosaic().resize(size.width, size.height).trim().gravity('Center').extent(size.width, size.height).stream('png', callback);
};

var resize = exports.resize = function resize(imagePath, size, callback) {
  size = _parseSize(size);

  imageMagick(imagePath).resize(size.width, size.height).stream('png', callback);
};