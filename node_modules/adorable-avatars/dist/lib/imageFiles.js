'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.pathFor = exports.allPaths = exports.allNames = exports.dirFor = undefined;

var _fs = require('fs');

var _fs2 = _interopRequireDefault(_fs);

var _path = require('path');

var _path2 = _interopRequireDefault(_path);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var DEFAULT_IMAGE_DIR = _path2.default.join(__dirname, '..', 'img');

var dirFor = exports.dirFor = function dirFor(type) {
  var imageDir = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : DEFAULT_IMAGE_DIR;

  if (type) return _path2.default.join(imageDir, type);else return imageDir;
};

var allNames = exports.allNames = function allNames(type) {
  var imageDir = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : DEFAULT_IMAGE_DIR;

  return _fs2.default.readdirSync(dirFor(type, imageDir)).filter(function (imageFileName) {
    return imageFileName.match(/\.png/);
  }).map(function (imageFileName) {
    return imageFileName.replace(/\.png/, '');
  });
};

var allPaths = exports.allPaths = function allPaths(type) {
  var imageDir = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : DEFAULT_IMAGE_DIR;

  var dir;
  dir = dirFor(type, imageDir);
  return allNames(type, imageDir).map(function (name) {
    return _path2.default.join(dir, name + '.png');
  });
};

var pathFor = exports.pathFor = function pathFor(type, fileName) {
  var imageDir = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : DEFAULT_IMAGE_DIR;

  return _path2.default.join(dirFor(type, imageDir), fileName + '.png');
};